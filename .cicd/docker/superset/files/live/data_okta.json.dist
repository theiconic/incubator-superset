{
  "web": {
    "client_id": "${SUPERSET_OKTA_CLIENT_ID}",
    "client_secret": "${SUPERSET_OKTA_CLIENT_SECRET}",
    "auth_uri": "${SUPERSET_OKTA_AUTH_URI}",
    "token_uri": "${SUPERSET_OKTA_TOKEN_URI}",
    "token_introspection_uri": "${SUPERSET_OKTA_TOKEN_INTROSPECTIVE_URI}",
    "issuer": "${SUPERSET_OKTA_ISSUER}",
    "userinfo_uri": "${SUPERSET_OKTA_USERINFO_URI}",
    "redirect_uris": [
      "https://${SERVICE_URL}/oidc_callback"
    ]
  }
}