apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}
  labels:
    app: ${APP_NAME}
spec:
  ports:
    - protocol: TCP
      name: "https"
      port: 443
      targetPort: 443
  selector:
    app: ${APP_NAME}
  type: ClusterIP
